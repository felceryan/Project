#include <stdio.h>
// answer = ( 3*q-p*p ) /3 ;
int main(void){
    int x, y, z;
    float a, b, c;
    
    printf("x = %d\ny = %d\nz = %d\n",x, y, z);
    printf("a = %f\nb = %f\nc = %f\n",a, b, c);

    return 0;
}